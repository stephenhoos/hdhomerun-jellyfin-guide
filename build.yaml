---
name: "HDHomeRun Guide"
guid: "dedaf64f-c6d3-4b51-b88e-64873ec088a5"
version: "0.3.0.0"
targetAbi: "10.9.0.0"
framework: "net9.0"
overview: "Server-side HDHomeRun guide refresh for Jellyfin"
description: >
  Downloads SiliconDust HDHomeRun XMLTV data with the tuner DeviceAuth token
  and writes Jellyfin-compatible XMLTV and M3U files from a scheduled server task.
category: "Live TV"
owner: "Stephen Hoos"
artifacts:
- "Jellyfin.Plugin.HDHomeRunGuide.dll"
changelog: >
  Migrates guide retrieval to SiliconDust's XMLTV API and removes the
  render-oriented guide API reconstruction path. Reads fresh DeviceAuth for
  each refresh, can explicitly request the paid 14-day XMLTV window, accepts
  compressed XMLTV responses, and randomizes automatic refresh timing.
